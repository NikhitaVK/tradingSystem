# Secrets
.env

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# macOS
.DS_Store

# Local database
trading_system.db

# Local DB backups (snapshots before destructive KB operations)
backups/

# Obsidian (personal layout only)
.obsidian/workspace.json
.obsidian/

# Claude local context (for now)
.claude/
# SQLite WAL/SHM (transient)
*.db-wal
*.db-shm

# Trial outputs: keep the chart + results as evidence, drop the scratch artefacts
trials_out/_empty_kb.db
trials_out/*.png
